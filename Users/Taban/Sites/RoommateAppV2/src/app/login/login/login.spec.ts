
import { ElementFinder } from 'protractor';

import { loginComponent } from './login.component';

describe('login: ', () => {
    describe('Events and Lifecycle', () => {
        beforeEach(() => {
            this.instance = new loginComponent();
        });

        it('Properties are defined', () => {
            // TODO
        });
    });
});
