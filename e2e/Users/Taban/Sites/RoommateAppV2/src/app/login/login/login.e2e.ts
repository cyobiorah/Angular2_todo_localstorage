import { login } from './login.protractor';

describe('login E2E', () => {

    let login: login = new login;

    beforeAll(() => {
    });

    it('should check that ...', () => {
        // expect(XXX).toBeTruthy();
    });
});
